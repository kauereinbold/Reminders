{
  "name": "reminders-mcp-server",
  "version": "1.0.0",
  "description": "MCP server exposing the Reminders API to AI assistants",
  "private": true,
  "type": "module",
  "bin": { "reminders-mcp-server": "dist/src/index.js" },
  "scripts": {
    "build": "tsc",
    "start": "node dist/src/index.js",
    "test": "npm run build && node --test dist/test/*.test.js",
    "test:e2e": "node --test e2e/*.e2e.test.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
